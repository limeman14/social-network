spring:
  datasource:
    url: jdbc:mysql://localhost:3306/social_network?useUnicode=true&characterEncoding=utf8&useJDBCCompliantTimezoneShift=true&useLegacyDatetimeCode=false&serverTimezone=UTC
    username: root
    password: ab33aa4c
#    initialization-mode: always
#    data: classpath:/bd/data.sql
  jpa:
    hibernate.ddl-auto: update
    generate-ddl: true
    properties.hibernate.dialect: org.hibernate.dialect.MySQL55Dialect

    properties:
      hibernate:
        globally_quoted_identifiers: true

jwt.token:
  secret: jwtappdemo
  expired: 3600000
